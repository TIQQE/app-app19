# {{APP_SLUG}}

Generated by the [PostNord App Platform](https://github.com/TIQQE/postnord-tpl-app-portal).

## Environments

| Environment | URL |
|-------------|-----|
| dev | `http://<slug>-dev.apps-tpl.internal` |
| tst | `http://<slug>-tst.apps-tpl.internal` |
| prd | `http://<slug>-prd.apps-tpl.internal` |

These URLs resolve inside the VPC via a private Route 53 zone. They are not publicly accessible.

## CI/CD

| Workflow | Trigger | What it does |
|----------|---------|--------------|
| `ci.yml` | Push to `main` | Builds the Docker image, tags with git SHA, pushes to ECR |
| `deploy.yml` | `ci.yml` succeeds | Force-redeploys the dev ECS service with the new image |
| `promote.yml` | Manual (`workflow_dispatch`) | Promotes a given image tag to `tst` or `prd` — Phase 1 stub, currently a no-op |

Every push to `main` automatically builds and deploys to dev. Promotion to tst and prd is always manual.

## Running locally

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8080
```

| Route | Response |
|-------|----------|
| `GET /` | `{"message": "Hello from <slug>!", "env": "dev"}` |
| `GET /health` | `{"status": "ok"}` |

`APP_SLUG` and `APP_ENV` are read from environment variables and default to `"app"` and `"dev"` if not set.

## App secrets

Runtime secrets are stored in AWS Secrets Manager at `/apps/<slug>/<env>/config`. The ECS task role for each environment has read access to its own path. Update secrets via the AWS console or CLI — ECS picks them up on the next deployment.

## Promoting to tst or prd

tst and prd ECS services start at 0 replicas. To promote:

1. Go to Actions → **Promote — manual environment promotion**
2. Select the environment and provide the image tag (git SHA from a successful `ci.yml` run)
3. Scale the ECS service up to 1 via the portal or AWS console (Phase 2 of the promote workflow will automate this)

---

> **Template note:** If you are reading this in the `postnord-template-webapp` repository itself (before the portal has generated an app from it), the `{{APP_SLUG}}` and `<slug>` placeholders above are intentional. `{{APP_SLUG}}` in the heading is replaced during provisioning; the `<slug>` references in the body are plain instructions for the developer who receives the generated repo.
