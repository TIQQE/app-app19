name: Deploy — dev environment

on:
  workflow_run:
    workflows:
      - "CI — Build and push image"
    types:
      - completed
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy-dev:
    name: Deploy to ECS dev
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::061836593621:role/app-app19-github-deploy
          aws-region: eu-north-1

      - name: Update ECS service with new image
        env:
          IMAGE_URI: 061836593621.dkr.ecr.eu-north-1.amazonaws.com/app-app19:${{ github.event.workflow_run.head_sha }}
          CLUSTER: postnord-tpl-cluster
          SERVICE: app-app19-dev
          AWS_REGION: eu-north-1
        run: |
          TASK_DEF_ARN=$(aws ecs describe-services \
            --cluster "$CLUSTER" \
            --services "$SERVICE" \
            --region "$AWS_REGION" \
            --query 'services[0].taskDefinition' \
            --output text)

          NEW_TASK_DEF=$(aws ecs describe-task-definition \
            --task-definition "$TASK_DEF_ARN" \
            --region "$AWS_REGION" \
            --query 'taskDefinition' \
            --output json | jq --arg IMAGE "$IMAGE_URI" \
            'del(.taskDefinitionArn,.revision,.status,.requiresAttributes,.compatibilities,.registeredAt,.registeredBy,.deregisteredAt)
             | .containerDefinitions[0].image = $IMAGE')

          NEW_TASK_DEF_ARN=$(aws ecs register-task-definition \
            --cli-input-json "$NEW_TASK_DEF" \
            --region "$AWS_REGION" \
            --query 'taskDefinition.taskDefinitionArn' \
            --output text)

          aws ecs update-service \
            --cluster "$CLUSTER" \
            --service "$SERVICE" \
            --task-definition "$NEW_TASK_DEF_ARN" \
            --region "$AWS_REGION"

      - name: Wait for deployment to stabilise
        env:
          CLUSTER: postnord-tpl-cluster
          SERVICE: app-app19-dev
          AWS_REGION: eu-north-1
        run: |
          aws ecs wait services-stable \
            --cluster "$CLUSTER" \
            --services "$SERVICE" \
            --region "$AWS_REGION"
